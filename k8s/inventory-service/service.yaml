apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: shoptech
spec:
  selector:
    app: inventory-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP   # solo accesible internamente desde order-service
