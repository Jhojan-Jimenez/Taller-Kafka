apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: shoptech
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: LoadBalancer   # IP pública para que el cliente pueda hacer POST /orders
